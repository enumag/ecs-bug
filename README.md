Run `vendor/bin/ecs check src --fix`. It will write this result:

```
 [OK] No errors found. Great job - your code is shiny in style!
```

However the annotation in src/Foo.php will be now removed - this should not happen.

